# from itertools import count
# from itertools import repeat


# count()  Used to count with start and step as variables(infinite)
# repeat() Used to repeat an object with object and max repeats(opt) as vars
# cycle()  Used to cycle through an object with obj and cycle_len(opt) as vars


# accumlate  current sum in the list
# chain      concatnate objects
# chain.from_iterable convert 2-d list to 1-d
# compress   pass a object and a boolean object of same type
# return all true elem
# pairwise   pair adjacent elements
# product    returns obj1*obj2
# permutations returns permutations
# combinations returns combinations
